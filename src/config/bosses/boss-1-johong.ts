/**
 * B1 조총 방진 대장 — 스펙 §10.2
 *
 * HP 1450 / 2페이즈 / 목표 격파 24초. §14.4 검산에서 여유 −4%로 빠듯하지만 유지한다 —
 * 24초가 25초가 될 뿐이고, 첫 보스가 약간 버텨 주는 편이 학습 구간에 맞다(§19).
 *
 * 설계 의도(§10.2): 궤적 예고 없이 직선탄만 쓴다. 탄이 어디서 나오는지는 예비동작으로
 * 알려주되 어디로 가는지는 탄 자체로만 읽게 해서 패리 타이밍의 기본형을 가르친다.
 * 그래서 이 보스의 패턴 목록에는 예고 도형을 요구하는 kind가 하나도 없다 — charge도 mortar도
 * 없고, 유일한 접근 위협인 연막 돌격조차 방향선을 E-D 쪽이 갖는다. 여기에 예고 도형을 하나라도
 * 얹으면 §10.2가 이 보스에 준 유일한 교육 목적이 사라진다.
 */
import type { BossDef } from '../types';

/**
 * §10.2 부하 조총병 라인의 가로 폭 (u)
 *
 * 목업 `02_boss1_johongbangjin/scene.js:16`이 발사원을 x = 180 · 360 · 540 · 720 · 900에 두었다.
 * 간격 180u × 4구간 = 720u다. 04_밸런스_데이터_설계.md §8.2는 560을 적었으나 출처가 없고,
 * 발사원 폭은 화면에 그려지는 5기의 위치와 반드시 같은 값이어야 한다 —
 * 10_스펙_목업_불일치.md A14가 이 5기를 배경 장식이 아니라 실체 충돌 대상으로 만들었으므로,
 * 두 값이 갈리면 "보이는 곳이 아닌 데서 탄이 나온다"가 된다. render/boss.ts도 이 값을 읽는다.
 *
 * 5기는 B1의 단일 HP 풀을 공유한다(A14). 개별 HP도 개별 게이지도 개별 파괴도 없으므로
 * `BossPartDef`가 아니고, 그래서 이 파일에 `parts`가 없다 — §15.1이 개별 게이지를 요구한 부위는
 * B3 포문뿐이다.
 */
export const B1_GUNNER_LINE_SPAN_U = 720;   // §10.2 (u)

export const BOSS_1 = {
  id: 'B1',
  name: '조총 방진 대장',
  hp: 1450,                     // §10.2 · §10.7 요약표
  targetKillSec: 24,            // §10.2 목표 격파 시간 (s). §14.4 검산의 기준값이다
  shape: 'commander',           // §10.2 render/boss.ts의 실루엣 키
  hitBox: { wU: 470, hU: 290 }, // §10.2 목업 02_boss1_johongbangjin/scene.js:14 실측 (u). 원이 아니라 사각형이다

  phases: [
    {
      // §10.2 페이즈 1 (HP 100% → 55%)
      hpThreshold: 1.00,          // §10.2 시작 페이즈
      onEnter: null,
      // §10.1 페이즈의 패턴 목록은 순환한다. 무작위 선택이 아니다.
      // 스펙 §10.2 — `일제사격 → 엄폐 전진 → 반복`. 배열 순서가 곧 그 순환 순서다.
      patterns: [
        {
          name: '일제사격',
          // §10.2 부하 조총병 라인 5기가 예비동작 0.9초 → 동시 5발 직선. 2회 반복, 간격 1.4초.
          // aim이 'player'가 아닌 이유: §10.2가 조준을 명시한 것은 엄폐 전진 한 줄뿐이고,
          // 두 패턴을 가르는 축이 그 대비다. 목업 scene.js:27은 5발을 모두 조준으로 쐈지만
          // 스펙 문장이 이긴다.
          kind: 'volley', bullet: 'P2', count: 5,
          tellSec: 0.9,           // §10.2 발사 예비동작 (s). 스펙이 이 패턴에 직접 준 값이다
          intervalSec: 1.4,       // §10.2 반복 사이 간격 (s)
          repeat: 2,              // §10.2 2회 반복
          aim: 'down',
          sourceSpread: B1_GUNNER_LINE_SPAN_U,
          // §10.4의 'parts'는 파괴 가능 포문에서 나가는 발사다. B1의 조총병 5기는 부위가 아니라
          // 단일 HP 풀을 공유하는 실체이므로(A14) 발사원은 본체 쪽이고, 라인 위 어디서 나가는지는
          // sourceSpread가 정한다.
          from: 'boss',
        },
        {
          name: '엄폐 전진',
          // §10.2 본체가 좌↔우 왕복 이동하며 플레이어 조준 단발 3연사, 간격 0.35초.
          // 좌우 왕복은 발사 파라미터가 아니라 본체 이동이라 BossPattern에 자리가 없다 —
          // §10.1이 허용한 보스 좌우 이동이고 sim/boss-runner.ts가 갖는다.
          kind: 'volley', bullet: 'P2', count: 1,
          // 0.35가 아니라 0.95다. 스펙이 이 패턴의 예비동작을 직접 주지 않았고 단발 직선이므로
          // §14.1의 스테이지 1 값을 읽는다(10_스펙_목업_불일치.md A15). 그리고 이 값은 연사
          // 전체 앞에 한 번이지 repeat마다 다시 붙지 않는다(types-content.ts의 tellSec 계약 ①).
          tellSec: 0.95,          // §14.1 S1 단발 직선 발사 예비동작 (s)
          intervalSec: 0.35,      // §10.2 연사 간격 (s)
          repeat: 3,              // §10.2 3연사
          aim: 'player',
          sourceSpread: 0,        // §10.2 본체 한 점에서 나간다 (u)
          from: 'boss',
        },
      ],
    },
    {
      // §10.2 페이즈 2 (HP 55% → 0%)
      hpThreshold: 0.55,          // §10.2 진입 HP 비율
      onEnter: null,
      // 스펙 §10.2 — `일제사격 강화 → 연막 돌격 → 난사 → 반복`. 셋이 같은 자격의 순환 패턴이라
      // 연막 돌격도 이 배열에 들어간다. 진입 훅으로 빼면 E-D 4기가 페이즈당 1회로 끝나
      // 순환에서 영영 빠진다(12_통합_계약.md §10 E-09-정정).
      patterns: [
        {
          name: '일제사격 강화',
          // §10.2 예비동작 0.6초, 7발, 탄속 ×1.1. 반복 횟수와 간격은 스펙이 다시 적지 않았으므로
          // 페이즈 1의 일제사격을 그대로 물려받는다 — "강화"가 바꾼 것은 적힌 셋뿐이다.
          // 발사원 폭도 같은 라인이다. 5기가 7발을 쏘므로 총구는 라인 위에서 120u 간격이 된다.
          kind: 'volley', bullet: 'P2', count: 7,
          tellSec: 0.6,           // §10.2 발사 예비동작 (s)
          intervalSec: 1.4,       // §10.2 페이즈 1과 같은 반복 간격 (s)
          repeat: 2,              // §10.2 페이즈 1과 같은 반복 횟수
          aim: 'down',
          sourceSpread: B1_GUNNER_LINE_SPAN_U,
          from: 'boss',
          speedMul: 1.1,          // §10.2 탄속 배수. bullets.ts의 P2 속도 × difficulty의 S1 배율에 다시 곱한다
        },
        {
          name: '연막 돌격',
          // §10.2 화면 상단에서 E-D 4기 진입. 돌진 방향선 1.0초는 E-D 자신의 behavior('charge',
          // §8.1)가 갖고 있으므로 여기서 다시 적지 않는다 — 두 곳에 적으면 한쪽만 늙는다.
          // F-1(종대 강하)인 이유: §8.2의 여섯 중 "상단에서 진입"이면서 4기가 시차를 두고
          // 도착하는 것은 F-1뿐이고, 동시 도착이면 회피 공간이 한 번에 사라진다(HR-04).
          kind: 'summon', enemy: 'E-D', count: 4,   // §10.2 E-D 4기
          formation: 'F-1',                          // §8.2 종대 강하
        },
        {
          name: '난사',
          // §10.2 20발을 무작위 각도로 1.5초에 걸쳐 확산 발사. 발사원은 본체다 —
          // 상단 전역 낙하는 §10.5 불화살 비 쪽이고 그것만 origin이 'topSpan'이다.
          kind: 'barrage', bullet: 'P2', count: 20,  // §10.2 20발
          durationSec: 1.5,       // §10.2 확산에 걸리는 시간 (s)
          speedUPerSec: 550,      // §10.2 이 패턴 전용 탄속 (u/s). P2 기본 760이 아니다
          origin: 'boss',
        },
      ],
    },
  ],

  // §10.1 공통 규칙 셋(phaseTransitionSec · patternGapSec · deathSec)은 여기 없다.
  // 다섯 보스가 같은 값이라 bosses/index.ts의 BOSS_COMMON이 갖는다(12_통합_계약.md §10 E-12).
} as const satisfies BossDef;
