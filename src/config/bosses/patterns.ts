/**
 * 보스 패턴 프리미티브의 고정값 — 스펙 §6.2 · §10
 *
 * 타입 `BossPattern` 자체는 여기 없다. `types.ts`(실제 정의는 `types-content.ts`)가 갖고,
 * 이 파일은 그 union의 필드가 요구하는 값 중 **스펙이 보스와 무관하게 고정으로 못 박은 것**과
 * **스펙이 아예 정하지 않아 다섯 보스가 같은 규약을 써야 하는 것**만 갖는다. boss-1 ~ boss-5가
 * 여기서 읽어 가므로, §6.2의 한 줄을 고칠 때 고칠 곳이 보스 파일 다섯이 아니라 이 파일 하나다.
 *
 * 프리미티브 9종이 스펙의 어느 패턴을 표현하는가 — 필드별 의미는 `types-content.ts`가 적고
 * 여기는 대조표만 둔다.
 *
 *   volley    §10.2 일제사격 · 엄폐 전진 · 일제사격 강화 │ §10.4 좌현 일제포격 · 갑판 조총 사격 ·
 *             교차 포격 │ §10.5 대통 직사 │ §10.6 총력 일제사격 · 전현 포격
 *   spread    §10.3 투척 수리검 │ §10.4 최후 포격의 함포 6발 │ §10.5 화차 일제사격
 *   ring      §10.3 돌진 종료 링 │ §10.4 최후 포격의 신관 링 │ §10.5 곡사 폭발 링 │
 *             §10.6 전탄 발사 · 이도류 돌진 종료 링
 *   arc       §10.3 참격파 · 참격파 강화 · 이도류 난무 │ §10.6 총대장 등판
 *   charge    §10.3 돌진 베기 · 돌진 베기 + 수리검 │ §10.4 충각 돌진 │ §10.6 이도류 돌진 · 충각 돌진
 *   mortar    §10.5 곡사 포격 · 연쇄 포격 · 초토화 │ §10.6 화공 총공세
 *   barrage   §10.2 난사 │ §10.5 불화살 비 │ §10.6 난사 강화
 *   parallel  발사가 아니라 결합자다. §10.4 최후 포격 │ §10.5 초토화 │ §10.6 전탄 발사 · 화공 총공세
 *   summon    §10.2 연막 돌격 하나뿐이다. 사용처가 하나여도 스펙 §10.2의 순환 요구가
 *             「두 곳 이상」 휴리스틱을 이긴다(12_통합_계약.md §10 E-09-정정)
 *
 * `lance`는 이 목록에 없다 — 웨이브 위협이라 `LanceDef`이고 `sim/lance.ts`가 돌린다(12 §10 E-10).
 * §10.1 공통 규칙 셋(`phaseTransitionSec` · `patternGapSec` · `deathSec`)도 없다 —
 * `bosses/index.ts`의 `BOSS_COMMON`이 갖는다(12 §10 E-12).
 */

/**
 * §6.2 확산·링 발사의 예비동작 (s)
 *
 * §14.1의 스테이지별 값은 "단발 직선 (P1~P3)" 행에만 걸린다. 확산과 링은 스테이지와 무관하게
 * 이 값이며, 목업이 여기에 스테이지 값을 쓴 것이 10_스펙_목업_불일치.md A15의 오류다.
 * 같은 §6.2 행을 잡몹 쪽에서는 `difficulty.ts`의 `SPREAD_TELL_SEC`가 읽는다.
 */
export const PATTERN_SPREAD_TELL_SEC = 0.4;      // §6.2 (s)

/** §6.2 참격파(P5)의 백스윙 (s). 스테이지와 무관하게 고정이고 궤적 잔상을 미리 그리지 않는다 */
export const PATTERN_ARC_BACKSWING_SEC = 0.7;    // §6.2 (s)

/**
 * §6.2 본체 돌진의 방향선 표시 시간 (s)
 *
 * HR-05가 예고 도형을 붙이도록 허용한 셋 중 하나다. `telegraph.ts`의 `shapes.dash.durationSec`가
 * 같은 §6.2 행을 도형 쪽에서 읽으므로 두 값은 언제나 같아야 한다.
 */
export const PATTERN_CHARGE_TELEGRAPH_SEC = 1.0; // §6.2 (s)

/** §6.2 곡사(P7) 착탄 예고 원의 표시 시간 (s). `telegraph.ts`의 `shapes.impactCircle`과 같은 행이다 */
export const PATTERN_MORTAR_TELEGRAPH_SEC = 1.2; // §6.2 (s)

/**
 * 링 탄막의 첫 탄 각도 (deg) — §10.3 · §10.5 · §10.6
 *
 * 스펙이 링의 시작 각도를 한 번도 정하지 않았다. 등간격 전방위라 어디서 시작해도 밀도는 같지만,
 * 보스마다 다른 값을 쓰면 같은 링이 보스마다 다르게 보인다. 각도 규약은 `core/`의 `atan2`와 같다 —
 * +x 방향이 0°이고 화면 좌표계(y가 아래로 증가)에서 시계 방향이 양수다. 따라서 0°는 오른쪽,
 * 90°가 플레이어 쪽(아래)이다.
 */
export const PATTERN_RING_START_ANGLE_DEG = 0;   // §10.3 (deg)
