/**
 * 색 토큰 — 스펙 §17
 *
 * 색 하나가 게임 규칙 하나를 담는다. 규칙 없는 색은 추가하지 않는다. 넣기 전에 답할 것:
 * "이 색이 나타내는 게임 규칙은 무엇인가."
 *
 * 그리고 색만으로 상태를 구분하지 않는다(§17). 패리 불가는 탁적에 더해 각진 다각형·대각
 * 해칭·파선 외곽이 붙고, 반사탄은 황에 더해 중앙의 불투명 점과 additive streak가 붙는다
 * (12_통합_계약.md §7.2).
 *
 * 값은 승인된 목업의 tokens.css와 engine.js의 C 표에서 왔다. tokens.css 자체는 이식하지
 * 않는다 — 게임은 canvas에 그리므로 CSS 변수를 읽을 일이 없고, 색의 단일 소스가 둘이 되면
 * 반드시 어긋난다(12_통합_계약.md §3.2). 페이지 셸(body 배경·레터박스)이 쓰는 몇 색은
 * index.html에 인라인한다.
 */
import type { Palette } from './types';

export const PALETTE = {
  // 흑 — 모든 것이 얹히는 바탕
  ink900: '#06070a', // §15.1 캔버스 바탕. 보스 HP 바의 페이즈 눈금도 이 색으로 바를 깎아낸다
  ink800: '#0b0d13', // §17 반사탄 중앙의 불투명 점. additive는 위로만 올리므로 이 구멍은 어떤 더미 위에서도 남는다
  ink700: '#12161f', // §9.2 배경 구조물의 아래쪽 그늘. 성벽이 하늘에서 떨어져 보이게 하는 한 단 밝은 흑

  // 적 (jeoksa) — 적 탄환 · 적 본체. 닿으면 라이프 −1
  jeok: '#e23c2e', // §17 적 탄환 본체 · 적 글로우 · 보스 HP 바
  jeokHot: '#ffd9d2', // §17 발사 예비동작. 이 색으로 밝아지면 곧 쏜다는 뜻이다
  jeokDim: '#8e241a', // §17 갑주 이음새·바퀴살 등 형태 전용. 규칙 없음 — 타입이 그렇게 못 박은 유일한 항목이다

  // 황 (jahwang) — 반사탄. 내 데미지원이지만 나에게도 유효하다(HR-07)
  hwang: '#f5b324', // §17 반사탄 본체·글로우·streak. 콤보 숫자와 ×N 팝업도 이 색이다
  hwangHot: '#fff3d0', // §17 적 피격 플래시. 반사탄이 유효했다는 확인

  // 청 (byeokcheong) — 플레이어가 소유한 모든 것. 패리 원, 쿨다운 링, 라이프
  cheong: '#35b7e0', // §15.1 코어·패리 원·쿨다운 링·무적 링·라이프 pip·진행도 바
  cheongDim: '#17607a', // §15.1 갑주 안쪽 면과 쿨다운 링의 미충전 구간. 같은 청 계열의 어두운 단

  // 백 (hobun) — 예고 표시 3종 + 텍스트 + 방패병의 판정면
  baek: '#ede6d8', // §6.2 예고 도형 3종 · §15.1 점수 · §17 임팩트 프레임 채움색과 집중선
  baekMute: '#9aa3b2', // §15.1 라벨과 보조 텍스트. 값보다 한 단 낮은 위계
  baekFaint: '#5d6675', // §15.1 단위·머리글 텍스트, 그리고 §5.4 빈 패리의 「헛침」 팝업

  // 탁적 — 패리 불가. 반드시 각진 형태 + 해칭과 함께 쓴다
  takjeok: '#7a2b1e', // §4.3 장판 채움과 장판 글로우. 적(jeok)과 계열이 같아 색만으로는 구분되지 않는다

  // 카드 등급 — §11.2가 고정한 값이다. 다시 칠하지 않는다
  rarityNormal: '#d8d2c4', // §11.2
  rarityRare: '#3b82f6', // §11.2
  rarityEpic: '#a855f7', // §11.2
} as const satisfies Palette;

/**
 * 부팅 시 구울 글로우 스프라이트의 색 13종 — 06_렌더링과_게임필.md §3.1
 *
 * glow()가 언제나 반경 128로 부르므로 색당 한 장이면 된다. 목록 밖의 색을 요청하면 dev 모드
 * 오류를 낸다 — 전투 중 첫 등장 시점에 256×256 radial gradient fill이 한 번 들어가면 그
 * 프레임이 튀고, 그 프레임이 패리 프레임이면 판정 실패로 나타난다.
 *
 * 앞의 10종은 PALETTE를 참조한다. 뒤의 6종은 배경 전용이라 게임 규칙을 나르지 않아 PALETTE에
 * 자리가 없고, 그래서 여기가 그 색들이 사는 유일한 자리다.
 *
 * **등급색 셋도 여기 있어야 한다.** 전투 화면이 아니라 카드 선택 화면이 쓰는데
 * (`screens/card-select-card.ts`의 고른 카드 후광), 목록에 없으면 첫 ← → 나 첫 마우스 이동에서
 * dev는 던지고 릴리스는 1×1 빈 장을 돌려준다. 던지는 자리가 층 12라 그 프레임의 나머지가
 * 통째로 안 그려지고 `ctx.save()` 둘이 짝을 잃는다.
 */
export const GLOW_BAKE_COLORS = [
  PALETTE.hwang, // §17 반사탄
  PALETTE.jeok, // §17 적 탄환 · 적 본체 · 보스 HP 바 끝
  PALETTE.jeokHot, // §17 발사 예비동작 후광
  PALETTE.hwangHot, // §17 적 피격 플래시
  PALETTE.cheong, // §15.1 라이프 pip · 코어 · 패리 원
  PALETTE.baek, // §4.6 집중선 · §15.1 점수 글로우
  PALETTE.takjeok, // §4.3 장판
  PALETTE.rarityNormal, // §15.2 카드 선택 — 고른 카드의 후광
  PALETTE.rarityRare, // §15.2
  PALETTE.rarityEpic, // §15.2
  '#e2762e', // §9.2 S1 부산진 일출
  '#000000', // §9.2 S2 동래성 그을음. lighter 합성에서 검정은 0을 더하므로 실제로는 아무것도 안 그린다
  '#2f7fa8', // §9.2 S3 한산도 상단 수면광
  '#9fb6d8', // §9.2 S4 행주산성 달무리 · S5 노량 번개
  '#e8811f', // §9.2 S4 행주산성 횃불
  '#e2531f', // §9.2 S5 노량 왜선 화염
] as const satisfies readonly string[];
