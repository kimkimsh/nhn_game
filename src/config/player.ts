/**
 * 플레이어 파라미터 — 스펙 §4.1 · §4.2
 *
 * 이 표는 카드를 하나도 먹지 않은 상태의 기준값이다. 실제로 판정에 쓰는 값은 §11.6의 9단계
 * 합산을 거친 sim/stats.ts의 EffectiveStats이고, 여기의 여섯 칸 중 셋이 카드에 움직인다 —
 * 이동 속도(N02), 최대 라이프(R06), 피격 무적(N09). 그러니 여기를 고치면 카드의 체감폭도 같이
 * 움직인다. 상한은 parry.ts의 HARD_LIMITS가 따로 막는다.
 *
 * 여기 없는 것 둘. 피격 히트스톱 0.14초(§4.2 5단계)는 feel.ts의 HITSTOP_SEC.playerHit이
 * 유일 소스이고(12_통합_계약.md §10 E-13), 장판 피해 주기 0.5초(§4.3)는 feel.ts의
 * ZONE.damageIntervalSec다 — 장판 상한·지속 시간과 나란히 조정되는 값이라 그쪽이 맞는 자리다.
 */
import type { PlayerConfig } from './types';

export const PLAYER = {
  // §4.1 8방향 입력이고 대각선은 정규화한다 — 대각선이 빠르면 최적 이동이 대각선 하나가 된다.
  // 가속도 감속도 없다. 입력한 프레임에 곧바로 이 속도이며 놓는 즉시 0이다
  moveSpeedUPerSec: 520,  // §4.1 이동 속도 (u/s)

  hitRadiusU: 20,         // §4.1 피격 판정 반경 (u). 화면의 코어 점과 1:1이다 — 갈리면 §17이 깨진다
  visualSizeU: 70,        // §4.1 시각적 크기 (u). 판정과 무관하다

  startLife: 5,           // §4.1 초기 라이프. 5스테이지를 관통해 공유되고 클리어로 회복되지 않는다(§13.2)
  maxLife: 8,             // §4.1 카드로만 오른다(R06). 라이프 회복 카드 N14는 이 값에 막힌다

  // §4.2 3단계. 이 동안 적 탄환·적 본체·반사탄·장판이 전부 무해하고, 패리는 정상 동작한다
  hitInvulnSec: 1.5,      // §4.2 피격 무적 (s)

  // §4.2 2단계. 지우는 것은 적 탄환뿐이다 — 반사탄은 플레이어의 자산이라 남긴다(HR-07).
  // 피격을 일으킨 반사탄 자신만 예외로 사라진다. 이 소멸에는 점수가 붙지 않는다
  hitClearRadiusU: 280,   // §4.2 피격 시 적 탄환 소멸 반경 (u)
} as const satisfies PlayerConfig;
