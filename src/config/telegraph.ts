/**
 * 예고 도형과 발사 예비동작 — 스펙 §6.2 (HR-05 · HR-09)
 *
 * HR-05가 예고 도형을 3종으로 못 박았다 — 착탄 예고 원(P7) · 관통 예고선(P12) · 돌진 방향선(본체).
 * 이 셋의 공통점은 패리로 지울 수 없다는 것이고, 그래서 예고가 붙는다. 반대로 **일반 탄환에는
 * 어떤 예고 도형도 그리지 않는다** — 조준선도, 궤적선도, 착탄 원도 없다. 날아오는 탄을 보고
 * 받아치는 것이 이 게임의 기본 행위이고, 궤적을 그려 주면 그 판단이 통째로 사라진다.
 * 넷째 도형을 넣으려면 스펙 §2의 하드 규칙부터 고쳐야 한다.
 *
 * 예고(telegraph)와 예비동작(windup)은 다른 것이다. 예고는 **어디에** 닿을지를 도형으로 그리고,
 * 예비동작은 **언제** 나올지만 적 본체의 상태 변화로 알린다(§15.1). 이 파일이 둘을 함께 갖는
 * 이유는 §6.2 한 절이 둘을 함께 정의하고, 하나를 고칠 때 나머지가 같은 화면에서 대비되기 때문이다.
 */
import type { TelegraphConfig } from './types';

export const TELEGRAPH = {
  shapes: {
    impactCircle: {
      name: '착탄 예고 원',
      durationSec: 1.2,        // §6.2 곡사(P7) 착탄 지점 표시 (s)
      // 폭발은 원이 사라지는 순간 한 번 터진다. 판정이 머무는 구간이 없다
      activeSec: null,
    },
    pierceLine: {
      name: '관통 예고선',
      durationSec: 0.8,        // §6.2 폭 40u 예고선 (s)
      // 이 구간의 폭은 예고선과 같은 40u다(06 §5.4) — 소멸 연출에서 폭이 줄면 마지막 0.06초가
      // 실제보다 안전해 보인다.
      activeSec: 0.4,          // §6.1 예고선이 끝난 뒤 관통 판정이 머무는 시간 (s)
    },
    dash: {
      name: '돌진 방향선',
      durationSec: 1.0,        // §6.2 적 외곽 발광 + 방향선 (s)
      // 돌진은 본체 이동이라 판정이 지속되는 별도 구간이 없다. 접촉 판정은 본체가 갖는다
      activeSec: null,
    },
  },
  // 억제 거리 = 실제 탄속 × 이 값이고, 플레이어까지 거리가 그보다 가까우면 그 발사는 취소되고
  // 다음 주기를 기다린다. 최악은 P2 760 × S5 탄속 배율 1.34 = 1018 u/s → 458u다.
  // 예고 도형이 붙은 공격은 억제 대상이 아니다 — 그쪽은 도형이 이미 반응 시간을 준다.
  minFlightSec: 0.45,          // §6.2 HR-09 최소 비행 시간 (s)
} as const satisfies TelegraphConfig;

// 예비동작 세 값은 전부 이 파일 밖에 있다. 셋이 한 표에 모여야 서로 대비되기 때문이고,
// 그 표가 difficulty.ts다 — 단발 직선(P1~P3)은 스테이지마다 다른 STAGE_SCALING[stageId].tellSec,
// 확산·링은 고정값 SPREAD_TELL_SEC, 참격파(P5) 백스윙은 고정값 SLASH_TELL_SEC(§14.1 · §6.2).
// 스펙이 그 패턴의 예비동작을 직접 준 경우(B1 일제사격 0.9초 등)는 그 값이 이긴다
// (04 §8.1 tellSec 계약 ②).
