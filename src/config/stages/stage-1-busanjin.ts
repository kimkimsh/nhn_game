/**
 * 스테이지 1 부산진 새벽 — 스펙 §9.3
 *
 * 학습 목표 셋: 패리 입력, 등급 개념, 정면으로 받으면 되돌아간다는 반사 규칙. 세 웨이브가
 * 그 셋을 하나씩 맡는다.
 *
 * 이 스테이지만 동시 잡몹 상한 20기(§3.2)에 일부러 닿지 않는다. 규칙을 배우는 구간이라
 * 밀도보다 명료함이 우선이고, 도달치는 §14.1이 준 10기에서 멈춘다(§9.3). 편성을 늘리면
 * 학습 목표가 셋에서 넷이 된다.
 *
 * 웨이브 전환은 시간 기반이다(§9.1). 앞 웨이브의 적이 살아 있어도 startSec에 다음 웨이브가
 * 시작하고 잔존 적은 유지되므로, concurrentMax는 그 웨이브 편성의 합이 아니라 잔존분까지
 * 더한 도달치다.
 *
 * 스폰 좌표는 이 파일에 없다. 진입 방향·개체 간격·정지 y는 formation이 가리키는
 * formations.ts의 FormationDef가 갖는다(§8.2). 웨이브가 정하는 것은 어느 형태로 무엇을
 * 몇 기까지다.
 *
 * HR-02: 모든 웨이브에 원거리 적이 최소 1기 있어야 한다. W3은 E-B가 그 역할이다 —
 * E-D만 남기면 플레이어의 탄약 공급이 끊긴다.
 */
import type { StageDef } from '../types';

export const STAGE_1 = {
  id: 1, // §9.2 스테이지 1
  name: '부산진 새벽',
  background: 'busanjin',
  concept: '첫 상륙. 조총이라는 미지의 무기와의 조우',
  bossId: 'B1',

  waves: [
    {
      // §9.3 W1 — 조총병 4기만. 탄환 밀도를 최소로 유지해 패리 타이밍 하나만 배우게 한다
      startSec: 0,
      endSec: 13,
      formation: 'F-1', // §8.2 종대 강하
      squads: [{ enemy: 'E-A', count: 4 }],
      concurrentMax: 4, // §9.3 동시 최대
    },
    {
      // §9.3 W2 — 좌우에서 동시에 들어온다. 패리 원을 한 방향으로만 두면 안 된다는 것이 여기서 생긴다
      startSec: 13,
      endSec: 27,
      formation: 'F-2', // §8.2 좌우 협공
      squads: [
        { enemy: 'E-A', count: 5 },
        // 3발 부채꼴 P1이 처음 온다. 단발만 보던 눈에 등급 차이가 드러나는 자리다
        { enemy: 'E-B', count: 3 },
      ],
      concurrentMax: 8, // §9.3 동시 최대
    },
    {
      // §9.3 W3 — 돌격병 첫 등장. 패리로 풀리지 않는 위협을 회피로 처리하는 법을 배운다.
      // E-B가 횡대에 서고 E-D가 사인 곡선으로 내려오다 방향선을 띄운다(§8.1 charge)
      startSec: 27,
      endSec: 43,
      formation: ['F-3', 'F-4'], // §8.2 횡대 정렬 + 사인 곡선 강하
      squads: [
        // HR-02. 이 한 줄을 빼면 W3이 근접 전용 편성이 되어 탄약 공급이 끊긴다
        { enemy: 'E-B', count: 5 },
        // §14.1의 S1 근접·돌격 적 비율 20%가 이 5기다
        { enemy: 'E-D', count: 5 },
      ],
      concurrentMax: 10, // §9.3 동시 최대. §14.1의 S1 도달치와 같다
    },
  ],

  // §9.1 소강 구간 (s). 신규 적 탄환만 없을 뿐 무적 구간이 아니고, 잔존 반사탄은 그대로 유효하다.
  // 보스 B1 진입 시각 0:46(§9.3)은 마지막 웨이브 종료 43초에 이 3초를 더한 값이라 저장하지 않는다
  lullSec: 3, // §9.1

  // §9.1 잔존 잡몹 퇴장 유예 (s). 소강 3초 안에 화면을 떠야 하므로 3이다 —
  // 스펙의 4초는 소강보다 길어 "적 탄환 없음"을 깬다(01_설계_결정.md §4-B S-07)
  stragglerGraceSec: 3, // §9.1
} as const satisfies StageDef;
