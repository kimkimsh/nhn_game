/**
 * 플레이필드 좌표계와 전역 상한 — 스펙 §3.1 · §3.2
 *
 * 여기의 좌표는 전부 논리 단위(u)다. 픽셀이 아니다 — sim은 언제나 1080×1920 안에서 계산하고,
 * 실제 캔버스 크기에 맞추는 변환은 render/가 혼자 한다. 원점은 좌상단 (0,0)이고 x는 오른쪽,
 * y는 아래로 증가하므로 아래의 모든 y 구간은 "값이 클수록 화면 아래"다.
 *
 * §3.2의 항목 중 셋이 여기 없다. 동시 적 탄환 상한은 난이도 축 3이라 스테이지마다 다르고
 * difficulty.ts의 maxEnemyBullets 칸이 유일 소스다(12_통합_계약.md §10 E-13). 히트스톱 동시
 * 적용 불가는 값이 아니라 판정 규칙이라 sim/이 갖고, 초당 누적 상한 0.32초는 feel.ts의
 * HITSTOP_BUDGET_PER_SEC다.
 */
import type { PlayfieldConfig } from './types';

/**
 * §3.1 논리 해상도 (u). 9:16 세로 고정
 *
 * 이름을 둔 것은 아래에서 같은 숫자를 다시 쓰기 때문이다 — 돌격형 하한이 세로 해상도와 같고
 * 적 활동 영역의 오른쪽 끝이 가로 해상도와 같다. 리터럴로 세 번 적으면 해상도를 고친 사람이
 * 나머지 둘을 잊는다(04_밸런스_데이터_설계.md §1 규칙 5).
 */
const PLAYFIELD_WIDTH_U = 1080;   // §3.1
const PLAYFIELD_HEIGHT_U = 1920;  // §3.1

export const PLAYFIELD = {
  widthU: PLAYFIELD_WIDTH_U,    // §3.1
  heightU: PLAYFIELD_HEIGHT_U,  // §3.1

  playerBounds: {
    minXU: 70, maxXU: 1010,     // §3.1 플레이어 이동 가능 영역 x (u)
    minYU: 620, maxYU: 1830,    // §3.1 같은 영역 y (u). 위쪽 620u는 적과 보스의 자리다
  },

  /**
   * §3.1 적 활동 영역. minYU가 음수인 것은 화면 위에서 대열째로 내려오기 때문이다.
   *
   * 스펙은 이 영역의 x를 정하지 않았다(y만 적었다). 화면 폭 전체로 둔다 — 잡몹이 좌우로
   * 못 가는 자리가 스펙 어디에도 없기 때문이다. 다만 **x를 하드 클램프로 쓰면 안 된다**:
   * F-6 측면 통과(E-F)는 한쪽 밖에서 들어와 반대쪽 밖으로 빠지고(§8.2), 진입 궤적도 이 상자
   * 밖에서 시작한다. 화면 밖으로 나간 적을 지우는 것은 이 값이 아니라 despawnMarginU다.
   */
  enemyBounds: {
    minXU: 0, maxXU: PLAYFIELD_WIDTH_U,  // §3.1 (스펙 미지정 — 위 주석 참조)
    minYU: -200, maxYU: 1000,            // §3.1 적 활동 영역 y (u)
  },

  // §3.1 돌격형만 화면 바닥까지 내려온다. 나머지는 enemyBounds.maxYU 1000u에서 멈춘다
  chargerMaxYU: PLAYFIELD_HEIGHT_U,  // §3.1

  // §3.1 보스 정위치 영역. HR-08이 요구하는 "항상 상단" — 반사탄이 위로 가면 닿아야 한다
  bossHomeBounds: {
    minXU: 240, maxXU: 840,   // §3.1 보스 정위치 x (u)
    minYU: 180, maxYU: 520,   // §3.1 보스 정위치 y (u)
  },

  despawnMarginU: 150,      // §3.1 발사체 소멸 경계 (u). 플레이필드 외곽에서 이만큼 더 나가면 지운다

  // §3.2 상한에 닿으면 가장 오래된 반사탄부터 지운다. 신규 반사탄은 버리지 않는다 —
  // 패리는 언제나 성립해야 하기 때문이다
  maxReflectBullets: 240,   // §3.2 동시 반사탄 상한

  // §3.2 상한에 닿으면 신규 스폰을 지연시킨다. 편성은 취소되지 않는다(§9.1).
  // 20기는 성능 방어선이 아니라 연출 목표다 — 반사탄 한 발이 여러 기를 정리하는 장면을 만든다
  maxEnemies: 20,           // §3.2 동시 잡몹 상한
} as const satisfies PlayfieldConfig;
