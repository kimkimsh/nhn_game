# 환도 還刀

종스크롤 탄막 패리 액션. PC 브라우저 전용(스펙 §16.6).

```bash
npm install     # 최초 1회
npm run dev     # 개발 서버. 출력된 주소를 브라우저에서 연다
npm run build   # dist/index.html 한 장. 더블클릭으로 열린다
```

검증은 `npm run verify`(typecheck + test), 계층 규칙 검사는 `npm run lint`.
