# 난이도 조정 — config가 스펙과 갈라진 자리

소유자가 직접 플레이하며 값을 다시 세웠다. **지금 게임이 쓰는 값은 `src/config/`이고 스펙 §14는
그 유래다.** 이 문서가 둘 사이의 차이표다.

**일괄 배율은 없다.** 한 축씩 손으로 맞춘 값이라 어느 칸도 다른 칸에서 유도되지 않는다.
config 주석의 「원값」이 스펙이 적어 둔 숫자이고, 그 둘을 잇는 식은 존재하지 않는다.

## 차이표

### 스테이지 배율 — `config/difficulty.ts`

| | S1 | S2 | S3 | S4 | S5 |
|---|---|---|---|---|---|
| 적 HP 배율 | **1.0** (§14.1 1.00) | **1.2** (1.35) | **1.3** (1.80) | **1.4** (2.40) | **1.5** (3.10) |
| 탄속 배율 | **1.0** (1.00) | **1.0** (1.08) | **1.0** (1.16) | **1.0** (1.24) | **1.0** (1.34) |

`tellSec` · `maxEnemyBullets` · `waveCount` · `targetLengthSec` · `unparryable`은 §14.1 그대로다.

### 보스 HP — `config/bosses/boss-N-*.ts`

| | B1 | B2 | B3 | B4 | B5 |
|---|---|---|---|---|---|
| 지금 | **600** | **600** | **800** (+ 포문 200 × 4) | **1000** | **800** |
| §10.7 요약표 | 1450 | 2250 | 3200 (본체 2000 + 포문 300 × 4) | 3850 | 5100 |

`targetKillSec`(24 / 27 / 29 / 32 / 37초)은 §10 그대로 두었다.

### 탄환 — `config/bullets.ts`

BRP가 20 / 30 두 값으로 평탄해졌다. 저위력 탄이 크게 세지고 고위력 탄이 약해졌다.

| | P1 | P2 | P3 | P4 | P5 | P6 | P7 | P8 | P10 | P11 |
|---|---|---|---|---|---|---|---|---|---|---|
| BRP 지금 | 20 | 20 | 20 | 20 | 30 | 30 | 30 | 20 | 20 | 20 |
| §6.1 원값 | 10 | 14 | 12 | 12 | 30 | 55 | 70 | 12 | 11 | 16 |
| 속도 지금 | 550 | 800 | 900 | 700 | 500 | 400 | 350 | 500 | 650 | 400 |
| §6.1 원값 | 520 | 760 | 880 | 600 | 420 | 300 | 260 | 480 | 620 | 380 |

### 플레이어와 패리 — `config/player.ts` · `config/parry.ts`

| 항목 | 지금 | 스펙 |
|---|---|---|
| 패리 반경 | **120u** | §5.1 70u |
| 패리 반경 상한 | **180u** | §11.6 150u |
| 피격 판정 반경 | **20u** | §4.1 10u |
| 시각적 크기 | **70u** | §4.1 90u |
| 초기 라이프 | **5** | §4.1 3 |
| 최대 라이프 | **8** | §4.1 5 |
| 히트스톱 GREAT / GOOD / NOT BAD | **0.02 / 0.02 / 0.02초** | §5.3 0.075 / 0.05 / 0.02초 |

**패리 반경 상한을 150 → 180으로 올린 것이 이 표에서 유일하게 유도된 값이다.** 반경 120에
N03 3중첩(+18)과 R09(+18)를 더하면 156이라 150에 닿고, `tests/cards.test.ts`의 「카드 표로는
상한에 못 닿는다」 감시선이 울렸다. 24u 여유를 남긴 값이다.

### 잡몹 발수·주기 — `config/enemies.ts`

이 파일만 첫 조정값 그대로다(발수 × 0.7 반올림, 주기 × 1.4). E-B 3 → 2발, E-F 2 → 1발,
E-G 10 → 7발, E-H 2 → 1발, E-I 4 → 3발. 주기는 E-A 2.2 → 3.08초 등.

E-H는 1발이 되면서 `spreadDeg` ±10°가 무의미해졌다 — 부채꼴이 사라지고 조준 단발이다.

## 스펙에 없는 것을 넣은 자리 셋

전부 HR-03(적 탄환 공백 2.0초)을 지키기 위한 것이다. **HR-01상 적 탄환이 플레이어의 유일한
탄약이므로 그 공백은 규칙 위반이기 전에 데미지를 넣을 수단이 없는 구간**이다.

| 자리 | 무엇 | 근거 |
|---|---|---|
| `stages/stage-5-noryang.ts` W4 | E-I 3 → 5기 | 잔류형이 E-I뿐이라 잔존 3 + W5 편성 16 = 19로 상한 20에 못 닿는다. §9.7이 「신규 스폰이 지연되는 유일한 구간」이라고 부른 장면이 편성상 성립하지 않았다 |
| `sim/boss-telegraphs.ts` `fireChargeTrail` | 돌진 중 흘리는 탄이 **하강 구간만** 덮던 것을 예고·유지·복귀까지 넓혔다 | 돌진 한 사이클이 7초를 넘는데 그동안 발사가 0이었다 |
| `bosses/boss-3` · `boss-5` 충각 돌진 | `duringInterval: { bullet: 'P6', intervalSec: 0.8 }` 추가 | §10.4 · §10.6이 이 패턴에 발사를 주지 않았다 |
| `bosses/boss-3` 페이즈 2 | 페이즈 1의 「갑판 조총 사격」을 순환에 추가 | 그 페이즈의 스펙 패턴 둘 중 교차 포격이 `from: 'partsAlternating'`이라 포문 전멸 시 한 발도 안 나간다. §10.4의 「본체가 대신 쏘지 않는다」를 지키면서 본체 발사원을 따로 둔 것이다 |
| `sim/enemies.ts` `firstShotSec` | 분대의 첫 발을 분대 안의 자리로 주기의 0.35~1.0 구간에 흩는다 | 모든 개체가 `fireTimerSec = fireCycleSec`로 시작해 분대가 한 몸처럼 쏘고 한 주기를 통째로 쉬었다 |

마지막 것은 난수를 안 썼다. 난수는 같은 기댓값에서도 뭉치는 표본을 내고, 뭉치면 그 자리에 다시
공백이 생긴다. `spawn.memberIndex`가 이미 `planEntry`에 있던 값이라 새 데이터도 없다.

## 스펙과 어긋난 것으로 남겨 둔 것

**§14.4 검산표 전체.** 보스 격파 시간(24 / 27 / 29 / 32 / 37초), T-03의 「NOT BAD 3발 이내」,
T-01·T-02의 추정 DPS가 전부 원값 위에서 계산됐다. config를 다시 만질 때 근거로 쓰면 안 된다.

**§6.2 억제 거리 검산 458u.** 지금은 360u다(P2 800 × S5 1.0 × 0.45).

**§3.2 히트스톱 누적 상한 0.32초.** 등급 히트스톱이 셋 다 0.02초가 되면서 **어떤 카드 조합으로도
도달할 수 없다** — 가장 빠른 R10 빌드가 초당 0.167초로 상한의 52%다. 상한이 죽은 값이고,
`tests/guards.test.ts`가 그 사실을 단언으로 박아 두었다. 등급 표를 다시 올리면 거기가 먼저
빨간불이 되고 그때 `core/clock.ts`의 절단 경로가 살아난다.

스펙 본문은 고치지 않았다. 고치려면 §14.1 표 · §14.4 검산표 · §6.2 검산 · §8.1 아키타입 표 ·
§10.2~§10.7 보스 패턴이 함께 움직여야 하고, 그것은 밸런스 재설계이지 튜닝이 아니다.

## 실측 — `npm run balance`

**봇의 유효 패리/초가 §14.4 전제 2.08의 26%다. 아래는 봇의 수치이고 사람의 것이 아니다.**
봇은 x축으로만 정렬하고 반사탄을 쫓지 않는다.

| 항목 | 조정 전 | 지금 |
|---|---|---|
| 판정 실패 | 24건 | **7건** |
| HR-03 가드 위반 | 160회 | **3건** (전부 2.008초) |
| §3.2 · §9.7 도달치 실패 | 15건 | **0건** |
| S5 스폰 지연 | 0회 | 3회 |
| 스테이지 클리어 | 0회 | S3 2회 · S5 3회 |
| 보스 격파 | 0회 | B3 2회 · B5 3회 |

### 남은 실패 7건 — 전부 원인 확정

| 항목 | 건수 | 원인 |
|---|---|---|
| 09 §2.1 가드 | 3 | 웨이브 경계의 진입 공백. 아래 진단 참조 |
| §14.3 T-04 | 2 | S3 104.1초, 목표 90초에서 +15.6% |
| §14.3 T-05 | 1 | B5를 19.2초에 격파. 하한 20초 |
| 09 §1.2 완주 | 1 | `baseline-full`이 S1에서 죽는다 |

#### HR-03 3건 — 웨이브 경계에서 탄약이 끊긴다

계측한 값이다(`baseline-full` 시드 918273 · 60451, 둘 다 같은 자리):

```
{"atSec":15.2, "phase":"waves", "enemies":8, "live":8, "suppressed":0}
```

**적 8기가 살아 있고 HR-09에 막히지도 않았는데 탄이 없다.** 여덟이 전부 진입 중이기 때문이다 —
§8.1의 `holdAndFire`가 「진입 후 정지 → 예비동작 → 발사」라 진입이 끝나기 전에는 발사 주기가
돌지 않는다. S1 W2가 0:13에 스폰되고 첫 탄은 `진입 이동 + 예비동작 0.95초` 뒤에 나온다.

그 사이를 앞 웨이브의 잔탄이 덮어야 하는데, **앞 웨이브를 빨리 정리할수록 잔탄이 먼저 사라진다.**
잘하는 플레이일수록 벌어지는 공백이고, 웨이브 경계마다 전 스테이지에서 같은 구조로 생긴다.

위반 값은 2.008초로 상한 2.000초를 8밀리초 넘는다.

**고치지 않았다.** 근본 고침은 웨이브 스폰이 시작 시각보다 먼저 출발해 시작 시각에 도착하게
하는 것(`sim/waves.ts`의 `dueSec = wave.startSec + entryDelaySec`에 진입 이동 시간만큼의 선행을
빼는 것)인데, 그러면 전 스테이지 전 웨이브의 등장 타이밍이 앞당겨진다. 8밀리초를 위해 치르기엔
큰 대가라 소유자 판단으로 남겼다. 00_진행_상황.md의 R2가 「시작 구간 — 스펙 개정 대기」로 올려
둔 항목이고, 이번에 원인이 처음으로 확정됐다.

#### T-04 · T-05 · 완주 — 봇으로는 잴 수 없는 축

**T-04와 T-05의 목표치는 옮기지 않았다.** 두 실패가 서로 반대 방향을 가리키기 때문이다.

- T-04는 **봇이 약해서** 느린 것이다. 사람이 전제대로 패리하면 빨라진다. 목표를 104초로 올리면
  봇의 약함이 스펙에 박힌다. §14.3 T-04가 정한 조치도 목표 길이가 아니라 「적 HP 배율을 먼저
  조정한다」이고, 그것은 이미 체감으로 맞춘 값이다.
- T-05는 반대다. **약한 봇이 그런데도 19.2초에 잡았다.** 사람은 더 빠르다. 하한을 내리면 진짜
  신호를 덮는다. §14.3 T-05의 조치는 카드 계수를 낮추는 것이다.
- 완주 1건도 같다 — 봇이 S1에서 죽는 것이지 사람이 못 도는 것이 아니다.

그리고 격파 표본이 B3 2회 · B5 3회, B1 · B2 · B4는 0회다. 5중 3~4가 목표치를 다시 세울 근거
자체를 갖고 있지 않다.

### 리포트 쪽에서 고친 것 둘

값이 아니라 **재는 방법**이 틀렸던 자리다.

**`dev/report.ts`의 `peaksVerdict`** — §18.3으로 보스 페이즈에 바로 들어간 시나리오에도 S5의
웨이브 밀도(잡몹 ≥ 18 · 스폰 지연 ≥ 1)를 요구했다. 웨이브를 한 번도 안 돈 런에서 잡몹 0기는
「밀도가 모자랐다」가 아니라 「편성이 돌지 않았다」다. 잡몹이 한 기도 안 나온 스테이지는
`no-band`로 남긴다 — 15건 중 10건이 이 허수였다.

**`dev/metrics.ts`의 `trackSpawnDeferrals`** — 지연을 「상한이 풀린 뒤 예약이 줄었는가」로
사후 확인했다. 그런데 상한이 걸린 밀집 구간이 곧 플레이어가 죽는 자리라 상한이 풀리기 전에 런이
끝나고, 실제로 일어난 지연이 한 건도 안 세어졌다. 계측으로 확인한 값이 `maxPendingWhileFull`
4~5기 · 상한에 닿은 스텝 112~861개다 — 지연은 내내 일어나고 있었다. 큐가 시각순이므로
「나왔어야 하는 예약이 상한에 막혀 있다」로 그 스텝에서 바로 확정하게 바꿨다(`sim/waves.ts`의
`hasDueSpawn`).

## 게이트

| 검사 | 결과 |
|---|---|
| `npx tsc --noEmit` | 통과 |
| `npx eslint .` | 통과 |
| `npx vitest run` | 511건 중 510 통과 · 1 건너뜀 |
| `npm run build` | `dist/index.html` 242.7 kB |
| `npm run balance` | exit 1 — 실패 7건 |
| 시각 회귀 | 조정 이후 안 돌렸다 — 브라우저 드라이버 구간이고 정지 화면의 배치는 안 바뀌었다 |
